version: "3.8"

services:
  drivernos:
    image: resb53/drivernos:latest
    secrets:
      - drivernos-secret
    volumes:
      - .:/run/app/data:rw

secrets:
  drivernos-secret:
    external: true
